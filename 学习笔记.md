# 编程学习笔记

> 每天的学习内容归档，方便复习。新一天的内容会自动追加到文件末尾。

---

## Day 1｜环境搭建 + 第一行代码（2026-07-27）

### 今天学了什么
1. **GitHub** — 程序员的代码托管平台，注册了账号（github.com）
2. **freeCodeCamp (FCC)** — 免费编程学习社区，注册了账号（freecodecamp.org）
3. **VS Code** — 微软的代码编辑器（code.visualstudio.com）
4. **Cursor** — 带 AI 的代码编辑器，基于 VS Code（cursor.com）
5. 写了第一行 HTML 代码

### 关键命令/概念
| 概念 | 说明 |
|------|------|
| `F:\SoloProgramming\` | 你的学习工作目录，所有代码放这里 |
| `.html` | 网页文件扩展名，双击会用浏览器打开 |
| `<h1></h1>` | HTML 标签，显示一级大标题 |
| Ctrl + S | 保存文件 |

### 验收标准
- [x] GitHub 账号注册成功
- [x] FCC 账号注册成功
- [x] VS Code 安装成功
- [x] Cursor 安装成功
- [x] `1.html` 写入 `<h1>我开始学编程了</h1>`，浏览器能显示

### 我写的代码
```html
<h1>我开始学编程了</h1>
```

---

## Day 2｜Git Bash 安装 + 命令行初体验（2026-07-28）

### 今天学了什么
1. **Git Bash** — Windows 上的命令行终端，用键盘代替鼠标操作文件（gitforwindows.org）
2. 三个最基本的命令行命令
3. 用命令行找到昨天的代码文件

### 关键命令/概念
| 命令 | 全称 | 作用 | 示例 |
|------|------|------|------|
| `pwd` | print working directory | 显示当前在哪个文件夹 | `pwd` → `/c/Users/用户名` |
| `ls` | list | 列出当前文件夹里的内容 | `ls` → 显示所有文件和子文件夹 |
| `cd` | change directory | 进入某个文件夹 | `cd Desktop` → 进入桌面 |
| `cd ..` | — | 回到上一层文件夹 | `cd ..` → 返回上级目录 |

### 盘符表示法
- Windows 资源管理器：`F:\SoloProgramming\`
- Git Bash 里：`/f/SoloProgramming/`
- 规则：盘符变小写字母 + 去掉冒号 + 用 `/` 代替 `\`

### 验收标准
- [x] 安装 Git Bash，右键能看到 "Git Bash Here"
- [x] 会用 `pwd` 查看当前路径
- [x] 会用 `ls` 查看文件夹内容
- [x] 会用 `cd` 进入和退出文件夹
- [x] 用命令行找到 `/f/SoloProgramming/1.html`

### 我的实操记录
```
$ cd /f/SoloProgramming
$ ls
1.html  AICollaborationFramework/  'FreeCodeCampREADME (2).md'  'dev rdmp readme (1).md'
```

### 注意事项
- 命令不要背，敲几遍就记住
- `rm` 是删除命令，现在先别碰
- 命令输错不会弄坏电脑，大胆试

---

## Day 3｜命令行建文件 + Git 初体验（2026-07-29）

### 今天学了什么
1. 用命令行创建文件夹（`mkdir`）和文件（`touch`）
2. Git 初始配置（user.name / user.email）
3. Git 三步工作流：`git init` → `git add` → `git commit`

### 关键命令/概念
| 命令 | 作用 | 示例 |
|------|------|------|
| `mkdir 文件夹名` | 创建文件夹 | `mkdir my-first-project` |
| `touch 文件名` | 创建空文件 | `touch index.html` |
| `ls -a` | 显示所有文件（含隐藏） | 看到 `.git` 隐藏文件夹 |
| `git init` | 把文件夹变成 Git 仓库 | 初始化后生成 `.git` 目录 |
| `git config --global user.name` | 设置 Git 用户名（只需一次） | `git config --global user.name "用户名"` |
| `git config --global user.email` | 设置 Git 邮箱（只需一次） | `git config --global user.email "邮箱"` |
| `git status` | 查看文件状态 | 红色=未管理，绿色=已暂存 |
| `git add 文件名` | 把文件加入暂存区 | `git add index.html` |
| `git commit -m "说明"` | 提交快照 | `git commit -m "第一次提交"` |
| `git log` | 查看提交历史 | 显示所有 commit 记录 |

### Git 工作流（三步走）
```
工作区（你写的文件） → git add → 暂存区 → git commit → 仓库（拍快照）
     红色              git add    绿色     git commit   记录完成
```

### 验收标准
- [ ] 用 `mkdir` 创建 `my-first-project` 文件夹
- [ ] 用 `touch` 创建 `index.html` 并写入内容
- [ ] Git 全局配置完成（user.name / user.email）
- [ ] `git init` 初始化仓库成功
- [ ] `git add` + `git commit` 完成第一次提交
- [ ] `git log` 能看到提交记录
- [ ] `git status` 显示 `working tree clean`

### 注意事项
- `git config --global` 只需配置一次，所有项目共用
- `git init` 后会生成 `.git` 隐藏文件夹，别手动删它
- `git commit -m` 后面的说明要写清楚这次改了什么，别写"asdf"
- 今天只做本地提交，还没推到 GitHub（明天学 `git push`）

---

## Day 4｜代码推上 GitHub（2026-07-30）

### 今天学了什么
1. 在 GitHub 网站创建远程仓库
2. 用 `git remote add` 连接本地仓库和 GitHub
3. 第一次 `git push` 推送代码到云端
4. 掌握"改→add→commit→push"日常循环
5. GitHub 认证方式（Credential Manager 或 PAT）

### 关键命令/概念
| 命令 | 作用 | 示例 |
|------|------|------|
| `git remote add origin 地址` | 连接远程仓库 | `git remote add origin https://github.com/用户名/仓库名.git` |
| `git remote -v` | 查看已连接的远程仓库 | 显示 origin + 地址 |
| `git branch` | 查看当前分支名 | `main` 或 `master` |
| `git push -u origin 分支名` | 第一次推送，记住关联 | `git push -u origin main` |
| `git push` | 以后推送（简化版） | 不用再写全名 |

### 日常 Git 循环（核心）
```
改代码 → git add → git commit -m "说明" → git push
(工作区)  (暂存区)    (本地仓库)           (GitHub云端)
```

### GitHub 认证
- 方式A：Git Credential Manager 弹浏览器登录（推荐，最简单）
- 方式B：Personal Access Token (PAT) 当密码用
  - GitHub → Settings → Developer settings → Personal access tokens → Tokens (classic) → Generate
  - 勾选 repo 权限，设过期时间
  - 生成的 token 只显示一次，立刻复制

### 验收标准
- [x] GitHub 上创建了 `my-first-project` 仓库 ✅
- [x] `git remote add` 连接成功，`git remote -v` 能看到 origin ✅
- [x] `git push` 推送成功，GitHub 页面能看到 `index.html` ✅
- [x] 练习了第二次修改→提交→推送循环 ✅（2 次提交）
- [x] GitHub 页面能看到更新的内容 ✅

### 注意事项
- GitHub 不再接受密码登录命令行，必须用 PAT 或浏览器认证
- `git push -u` 只需第一次写全名，以后直接 `git push`
- 分支名可能是 `main` 或 `master`，`git branch` 确认（我的分支是 `master`）
- 远程地址以 `.git` 结尾
- **网络问题：** 如果 `git push` 报 `Failed to connect to github.com:443`，是 Git 没走代理。解决方法：
  ```bash
  git config --global http.proxy http://127.0.0.1:7892
  git config --global https.proxy http://127.0.0.1:7892
  ```
  （端口号换成你自己代理的端口）

---

## Day 5｜FCC 平台导览 + roadmap.sh 路线图速览 + Git 循环复习（2026-07-31）

### 今天学了什么
1. FCC 新版课程结构：Responsive Web Design 认证名没变，拆成 HTML + Computers + CSS 三个子模块 + 一个考试
2. roadmap.sh Frontend Beginner 精简版全貌：HTML → CSS → JavaScript → Git/GitHub → npm → React
3. 用 Git 把学习笔记推上 GitHub（复习 Day 4 的 Git 循环）

### FCC 结构（新版）
- **Responsive Web Design** = 大认证名没变
- 里面拆成 3 个子模块：
  - **HTML**（302 步）— 最前面，先学这个
  - **Computers / Computer Basics**（16 步）— 计算机基础常识
  - **CSS**（1234 步）— 样式
- 最后是 **Responsive Web Design Certification Exam**（考试）
- 每个子模块里有六种类型：
  - **Workshop** — 交互式编码练习，边做边学（如 Build a Curriculum Outline）
  - **Lab** — 需要独立完成的小项目（如 Debug Camperbot's Profile Page）
  - **Theory** — 文字/视频讲解，不用写代码（如 Understanding HTML Attributes）
  - **Review** — 复习总结课（如 Basic HTML Review）
  - **Quiz** — 测验题（如 Basic HTML Quiz）
  - **Certification Project** — 认证必做项目（如 Build a Survey Form），必须自己独立完成

### roadmap.sh Frontend Beginner（精简版）
- 所有节点为黄色，表示这是入门的核心学习路径
- 主干顺序：HTML → CSS → JavaScript → Git/GitHub → npm → React...
- 点每个节点会弹出详情页，有推荐资源和说明

### 验收标准
- [x] 理解 FCC 新版结构：大认证名没变，拆成 HTML/CSS/Computer Basics 三个子模块
- [x] 找到 HTML 子模块的第一个 Workshop（Build a Curriculum Outline）
- [x] 理解 Workshop / Lab / Theory / Review / Quiz / Certification Project 六种类型
- [x] 浏览 roadmap.sh Frontend Beginner 整张路线图，说出主干顺序
- [x] 把"学习笔记.md"用 Git 循环推到 GitHub

### 注意事项
- FCC 的课可以随时暂停，进度自动保存
- roadmap.sh 精简版比详细版清爽，先看这张
- 学习笔记（本文件）是接下来一年的重要资产，记得每天推 GitHub 备份

---

## Day 6｜Cursor 编辑器实操 + HTML 进阶练习（2026-08-01）

### 今天学了什么
1. 用 Cursor 打开项目文件夹（不是单个文件）
2. 编辑器核心操作：侧边栏、新建文件、自动保存、内置终端
3. 写一个比 Day 1 更复杂的 HTML 页面（多个标签组合）
4. Git 循环复习：add → commit → push

### Cursor 编辑器核心操作
| 操作 | 快捷键/方法 | 说明 |
|------|-------------|------|
| 打开文件夹 | File → Open Folder → 选 `my-first-project` | 打开整个项目，不是单个文件 |
| 新建文件 | 侧边栏右键 → New File | 或 Ctrl+N 新建未保存文件 |
| 保存 | Ctrl + S | 第一次保存会让你命名 |
| 自动保存 | File → Auto Save → 勾选 | 保存后改动自动写入磁盘 |
| 打开终端 | Ctrl + ` （反引号键） | 编辑器内置终端，不用另开 Git Bash |
| 命令面板 | Ctrl + Shift + P | 搜索任何编辑器功能 |
| 分屏 | 右上角拆分按钮 | 左右分屏，一边代码一边预览 |

### HTML 标签速查（今天用到的）
| 标签 | 作用 | 示例 |
|------|------|------|
| `<h1>~<h6>` | 标题（1最大6最小） | `<h2>关于我</h2>` |
| `<p>` | 段落文字 | `<p>我正在学编程</p>` |
| `<ul>` | 无序列表（圆点） | 包裹 `<li>` |
| `<ol>` | 有序列表（数字） | 包裹 `<li>` |
| `<li>` | 列表项 | `<li>HTML</li>` |
| `<a>` | 超链接 | `<a href="https://freecodecamp.org">FCC</a>` |
| `<img>` | 图片 | `<img src="图片路径" alt="描述">` |
| `<!-- -->` | 注释（浏览器不显示） | `<!-- 这是备注 -->` |

### 验收标准
- [x] 用 Cursor 打开 `my-first-project` 文件夹（不是单个文件）
- [x] 在编辑器里新建 `about-me.html`
- [x] 页面包含：至少2级标题、1个段落、1个列表、1个链接
- [x] 用编辑器内置终端（Ctrl+`）执行 git add → commit → push
- [x] GitHub 上能看到 `about-me.html`

### 注意事项
- 以后写代码都用 Cursor，别用记事本了
- 打开文件夹而不是单个文件 —— 这样侧边栏能看到所有文件，方便切换
- `href` 和 `src` 是属性（attribute），写在标签里面，格式：`属性名="值"`
- HTML 标签可以嵌套，但要注意开闭顺序：后开的先关 → `<p><b>文字</b></p>`

---

## Day 7｜W1 收尾：工具总检查 + 综合小练习（2026-08-01）

### 今天学了什么
1. W1 所有工具逐项检查，确保环境 100% 就绪
2. 整理项目文件，养成好习惯
3. 综合练手：用学过的所有 HTML 标签写一个页面，走完整 Git 循环

### W1 环境就绪清单
| 检查项 | 怎么检查 | 状态 |
|--------|----------|------|
| GitHub 能登录 | 打开 github.com，看右上角有没有你的头像 | ⬜ |
| FCC 能登录 | 打开 freecodecamp.org，看课程页面能不能正常显示 | ⬜ |
| Git Bash 能用 | 右键桌面选 Git Bash Here，输入 `git --version` | ⬜ |
| Cursor 能编辑 | 打开 Cursor，Folder 打开 `my-first-project` | ⬜ |
| Git 推拉正常 | `git push` 不报错，GitHub 能同步 | ⬜ |

### 文件整理
- `F:\SoloProgramming\` 根目录清理：把散落的文件归位
  - `1.html` → 移到 `my-first-project` 里
  - 其他无关文件（如 readme）→ 不想管的就别动，不乱删

### 今天写的代码（标签大集合）
```html
<!-- 标签练习页 -->
<h1>我的HTML技能清单</h1>

<h2>标题们</h2>
<h3>我是三级标题</h3>
<h4>我是四级标题</h4>

<h2>文本</h2>
<p>这是一段普通的段落文字，里面可以放<b>加粗</b>的内容。</p>

<h2>列表</h2>
<h3>无序列表（想学的东西）</h3>
<ul>
  <li>HTML</li>
  <li>CSS</li>
  <li>JavaScript</li>
</ul>

<h3>有序列表（学习顺序）</h3>
<ol>
  <li>环境搭建</li>
  <li>命令行基础</li>
  <li>HTML + CSS</li>
</ol>

<h2>链接</h2>
<p>去 <a href="https://www.freecodecamp.org">freeCodeCamp</a> 学习。</p>

<h2>图片</h2>
<!-- 暂时没有图片，知道标签怎么写就行 -->
<!-- <img src="图片地址" alt="图片描述"> -->
```

### 验收标准
- [ ] W1 五项工具检查全部通过（GitHub/FCC/Git Bash/Cursor/Git推送）
- [ ] `1.html` 移到 `my-first-project` 文件夹里
- [ ] 新建 `skill-list.html`，手敲上面的标签练习代码
- [ ] 浏览器打开能看到所有标签效果
- [ ] Git add → commit → push 到 GitHub

### W1 总结（就这样，不废话）
- 7 天前你连 GitHub 是什么都不知道
- 现在你能：注册账号、装工具、用命令行、写 HTML、用 Git 推代码到云端
- W2 开始学真正的命令行操作：`cp`、`mv`、`rm`、绝对/相对路径
- 别飘，你才刚把鞋穿好，还没上路

### 常见错误（Day 6 实战踩坑）

- **易混字符：** `1`（数字一）、`l`（小写字母L）、`I`（大写字母i）在很多字体里长得几乎一样
  - `<h1>` = heading（数字1）
  - `<ul>` = unordered list（字母L）
  - `<ol>` = ordered list（字母L）
  - `<li>` = list item（字母i）
  - **避免方法：** 写之前先念出来"ul = you el"，别只看形状
- **浏览器容错：** 写错标签（比如 `<u1>`）浏览器可能照样渲染，因为浏览器对未知标签很宽容。但这不是"代码对"，只是"浏览器友好"。换浏览器或复杂场景就会出问题
- **代码是写给机器解析的，不是给人看的** —— 拼写必须严格

---

## Day 8｜命令行进阶：cp / mv / rm + 绝对路径与相对路径（2026-08-02）

### 今天学了什么
1. 三个文件操作命令：`cp`（复制）、`mv`（移动/重命名）、`rm`（删除）
2. 绝对路径 vs 相对路径的区别和用法
3. 用命令行管理项目文件，不再依赖鼠标

### 关键命令/概念
| 命令 | 全称 | 作用 | 示例 |
|------|------|------|------|
| `cp 源文件 目标文件` | copy | 复制文件 | `cp a.txt b.txt` |
| `cp -r 源文件夹 目标文件夹` | copy recursive | 复制文件夹 | `cp -r old new` |
| `mv 源 目标` | move | 移动或重命名 | `mv a.txt day8/` 或 `mv a.txt b.txt` |
| `rm 文件名` | remove | 删除文件 | `rm b.txt` |
| `rm -r 文件夹名` | remove recursive | 删除文件夹 | `rm -r day8/` |
| `rm -i 文件名` | interactive | 删除前询问确认 | `rm -i b.txt` |

### 路径概念
| 类型 | 写法 | 例子 | 特点 |
|------|------|------|------|
| 绝对路径 | 从根目录 `/` 开始 | `/f/SoloProgramming/my-first-project` | 不管当前在哪，都指向同一个位置 |
| 相对路径 | 从当前目录开始 | `practice-day8/`、`../index.html` | 依赖你当前所在的位置 |

### 相对路径常用符号
| 符号 | 含义 | 例子 |
|------|------|------|
| `.` | 当前目录 | `cp file.txt ./backup/` |
| `..` | 上一层目录 | `cd ..` |
| `~` | 用户主目录（C:\Users\你的用户名） | `cd ~` |

### 今日实操流程（按顺序敲）
```bash
# 1. 用绝对路径进入项目文件夹
cd /f/SoloProgramming/my-first-project

# 2. 创建今天的练习文件夹
mkdir practice-day8

# 3. 进入文件夹（相对路径）
cd practice-day8

# 4. 创建测试文件
touch note.txt

# 5. 复制文件
cp note.txt note-copy.txt

# 6. 重命名文件
mv note-copy.txt note-renamed.txt

# 7. 移动文件到上一级，再移回来
mv note-renamed.txt ../
mv ../note-renamed.txt .

# 8. 删除练习文件
rm note.txt note-renamed.txt

# 9. 回到上一层
cd ..

# 10. 删除练习文件夹（里面已经空了）
rm -r practice-day8
```

### 验收标准
- [x] 用 `cp` 成功复制一个文件
- [x] 用 `mv` 成功重命名一个文件
- [x] 用 `mv` 成功把文件移到另一个文件夹，再移回来
- [x] 用 `rm` 删除文件，用 `rm -r` 删除空文件夹
- [x] 能解释：绝对路径和相对路径的区别
- [x] 能独立写出 `/f/SoloProgramming/my-first-project` 的 Git Bash 路径
- [x] 把今天改动的任意文件（比如新建后删除练习文件夹的状态）用 `git add → git commit → git push` 推上 GitHub

### 注意事项
- `rm` 和 `rm -r` 删除的文件**不会进回收站**，删之前确认路径
- 不确定的时候加 `-i`：`rm -i 文件名` 会问你 `remove 文件名?`，输入 `y` 再回车才删
- 绝对路径像快递地址，完整；相对路径像"隔壁"，依赖你站在哪
- 命令行没有撤销键，删除前多看一眼

---

## Day 9｜命令行提效：cat / echo / Tab补全 / 命令历史 / 通配符（2026-08-03）

### 今天学了什么
1. `cat` — 不打开编辑器，直接在终端查看文件内容
2. `echo` — 在终端打印文字，或把文字写入文件
3. Tab 自动补全 — 敲半个文件名按 Tab，终端帮你补全
4. 命令历史 — 上下方向键翻阅之前敲过的命令
5. 通配符 `*` 和 `?` — 一次操作多个文件

### 关键命令/概念
| 命令/操作 | 作用 | 示例 |
|-----------|------|------|
| `cat 文件名` | 在终端显示文件全部内容 | `cat note.txt` |
| `echo "文字"` | 在终端打印一行文字 | `echo "hello"` |
| `echo "文字" > 文件名` | 把文字写入文件（覆盖原有内容） | `echo "标题" > note.txt` |
| `echo "文字" >> 文件名` | 把文字追加到文件末尾（不覆盖） | `echo "第二行" >> note.txt` |
| `Tab` 键 | 自动补全文件名/路径 | 输入 `cd my` 按 Tab → 补全成 `cd my-first-project/` |
| `↑` / `↓` 方向键 | 翻阅历史命令 | 按上箭头调出上一条命令 |
| `history` | 显示所有敲过的命令列表 | `history` |
| `通配符 *` | 匹配任意数量字符 | `*.txt` = 所有 txt 文件 |
| `通配符 ?` | 匹配单个字符 | `file?.txt` = file1.txt、fileA.txt，不匹配 file12.txt |

### `>` 和 `>>` 的区别（重点）
```
echo "A" > file.txt    → file.txt 内容变成 "A"（覆盖！原来的内容没了）
echo "B" >> file.txt   → file.txt 内容变成 "A\nB"（追加，不影响已有内容）
```
记住：一个 `>` 是覆盖（危险），两个 `>` 是追加（安全）。

### Tab 补全使用场景
- 文件名太长不想打：`cd my-f` + Tab → `cd my-first-project/`
- 路径补全：`cd /f/So` + Tab → `cd /f/SoloProgramming/`
- 命令补全：输入 `git c` + Tab + Tab → 显示 checkout、commit、config 等候选
- **如果按一次 Tab 没反应，说明有多个匹配，再按一次 Tab 会列出所有候选**

### 今日实操流程（按顺序敲）
```bash
# 1. 进入项目文件夹
cd /f/SoloProgramming/my-first-project

# 2. 创建今天的练习文件夹
mkdir practice-day9
cd practice-day9

# 3. 用 echo 创建文件并写入内容
echo "这是第一行" > diary.txt

# 4. 追加第二行
echo "这是第二行" >> diary.txt

# 5. 用 cat 查看文件内容
cat diary.txt
# 预期输出：
# 这是第一行
# 这是第二行

# 6. 测试覆盖（> 会删掉原有内容）
echo "覆盖了" > diary.txt
cat diary.txt
# 预期输出：
# 覆盖了
# （原来的两行没了！）

# 7. 重新写内容
echo " Monday: 学了cat和echo" > diary.txt
echo " Tuesday: 学了Tab补全" >> diary.txt
echo " Wednesday: 学了通配符" >> diary.txt

# 8. 用通配符查看所有 txt 文件
ls *.txt

# 9. 创建多个测试文件
touch file1.txt file2.txt file3.txt fileA.txt fileB.txt

# 10. 用通配符 * 列出所有 file 开头的文件
ls file*.txt

# 11. 用通配符 ? 只匹配 file 后面单个字符的文件
ls file?.txt
# 预期：file1.txt file2.txt file3.txt fileA.txt fileB.txt

# 12. 按上方向键，调出上一条命令，改成 ls file??.txt 试试（匹配不到任何文件，因为文件名是 file + 1个字符）

# 13. 查看 today 的命令历史
history

# 14. 练习 Tab 补全：输入 cd ../my 然后按 Tab
cd ../my[按Tab]

# 15. 清理练习文件
rm *.txt
cd ..
rm -r practice-day9
```

### 验收标准
- [x] 用 `echo "文字" > 文件` 创建文件并写入内容
- [x] 用 `echo "文字" >> 文件` 追加内容，理解 `>` 和 `>>` 的区别
- [x] 用 `cat` 查看文件内容
- [x] 用 Tab 键补全文件名/路径至少 3 次
- [x] 用 `↑` 方向键调出历史命令
- [x] 用 `history` 查看命令历史
- [x] 用 `ls *.txt` 列出所有 txt 文件
- [x] 能说出 `*` 和 `?` 的区别
- [x] Git add → commit → push 推上 GitHub

### 注意事项
- `>` 是覆盖写入，会删掉文件原有内容！不确定就用 `>>`
- Tab 补全只在路径/文件名正确时才生效，打错了不会补全
- `cat` 适合看小文件，大文件会一次性刷满屏幕（后面学 `less` 分页查看）
- 通配符是命令行的核武：`rm *.txt` 会删掉当前目录所有 txt 文件，删之前想清楚

---

## Day 10｜管道符 `|` + 文件查看进阶（less / head / tail）+ grep 搜索（2026-08-04）

### 今天学了什么
1. 管道符 `|` — 把一个命令的输出传给另一个命令当输入
2. `less` — 分页查看大文件（不会一次性刷满屏幕）
3. `head` / `tail` — 只看文件开头几行 / 末尾几行
4. `grep` — 在文件里搜索关键词
5. `wc` — 统计文件的行数、词数、字符数

### 关键命令/概念
| 命令 | 作用 | 示例 |
|------|------|------|
| `命令A \| 命令B` | 把 A 的输出传给 B 当输入 | `ls \| grep txt` |
| `less 文件名` | 分页查看文件（可上下滚动） | `less note.txt` |
| `head -n 数字 文件名` | 看文件前 N 行 | `head -n 5 note.txt` |
| `tail -n 数字 文件名` | 看文件后 N 行 | `tail -n 3 note.txt` |
| `grep "关键词" 文件名` | 在文件里搜索包含关键词的行 | `grep "Monday" diary.txt` |
| `grep -i "关键词" 文件名` | 搜索（忽略大小写） | `grep -i "hello" note.txt` |
| `grep -n "关键词" 文件名` | 搜索并显示行号 | `grep -n "error" log.txt` |
| `grep -r "关键词" 文件夹` | 递归搜索文件夹内所有文件 | `grep -r "TODO" .` |
| `wc 文件名` | 统计行数/词数/字符数 | `wc note.txt` |
| `wc -l 文件名` | 只统计行数 | `wc -l note.txt` |

### 管道符 `|` 是什么
```
命令A 的输出  →  |  →  命令B 的输入
```
类比：水管接水管，第一个命令"流出来"的数据，直接流进第二个命令。
- `ls | grep txt` → 先列出所有文件，再从结果里筛出含"txt"的行
- `cat note.txt | grep "hello"` → 先读文件内容，再搜索"hello"
- `history | grep "git"` → 从命令历史里找所有含"git"的命令

### `less` 操作指南
| 按键 | 作用 |
|------|------|
| `↑` / `↓` | 上下滚动一行 |
| `空格` | 向下翻一页 |
| `b` | 向上翻一页 |
| `/关键词` | 搜索关键词 |
| `q` | 退出 less |

### 今日实操流程（按顺序敲）
```bash
# 1. 进入项目文件夹
cd /f/SoloProgramming/my-first-project

# 2. 创建今天的练习文件夹
mkdir practice-day10
cd practice-day10

# 3. 用 echo 写一个多行文件
echo "Monday: learn pipes" > schedule.txt
echo "Tuesday: learn less" >> schedule.txt
echo "Wednesday: learn grep" >> schedule.txt
echo "Thursday: practice" >> schedule.txt
echo "Friday: review" >> schedule.txt
echo "Saturday: project" >> schedule.txt
echo "Sunday: rest" >> schedule.txt

# 4. 用 cat 看全部内容
cat schedule.txt

# 5. 用 less 分页查看（文件短可能没分页效果，但练习操作）
less schedule.txt
# 在 less 里按 q 退出

# 6. 只看前 3 行
head -n 3 schedule.txt
# 预期：Monday / Tuesday / Wednesday

# 7. 只看后 2 行
tail -n 2 schedule.txt
# 预期：Saturday / Sunday

# 8. 搜索包含 "learn" 的行
grep "learn" schedule.txt
# 预期：Monday / Tuesday / Wednesday 三行

# 9. 搜索并显示行号
grep -n "learn" schedule.txt
# 预期：1:Monday... 2:Tuesday... 3:Wednesday...

# 10. 搜索忽略大小写
grep -i "MONDAY" schedule.txt
# 预期：Monday: learn pipes

# 11. 统计文件行数
wc -l schedule.txt
# 预期：7

# 12. 管道符：列出当前目录文件，筛出含 txt 的
ls | grep txt

# 13. 管道符：从命令历史里找含 grep 的命令
history | grep grep

# 14. 管道符：统计 schedule.txt 有多少行含 "learn"
grep "learn" schedule.txt | wc -l
# 预期：3

# 15. 创建更多文件测试递归搜索
touch todo.txt
echo "TODO: finish FCC HTML" > todo.txt
echo "TODO: push to github" >> todo.txt

# 16. 递归搜索当前文件夹所有文件里的 TODO
grep -r "TODO" .
# 预期：在 todo.txt 里找到两行

# 17. 清理练习文件
cd ..
rm -r practice-day10
```

### 验收标准
- [ ] 用 `less` 打开文件，能用 `q` 退出
- [ ] 用 `head -n 3` 看文件前 3 行
- [ ] 用 `tail -n 2` 看文件后 2 行
- [ ] 用 `grep "关键词" 文件名` 搜索到匹配的行
- [ ] 用 `grep -n` 显示搜索结果的行号
- [ ] 用 `grep -i` 做忽略大小写的搜索
- [ ] 用 `wc -l` 统计文件行数
- [ ] 用管道符 `ls | grep txt` 筛选文件列表
- [ ] 用管道符 `history | grep git` 从历史里找命令
- [ ] 用管道符 `grep "词" 文件 | wc -l` 统计匹配行数
- [ ] Git add → commit → push 推上 GitHub

### 注意事项
- 管道符 `|` 和大于号 `>` 容易混：
  - `|` 是把输出传给**另一个命令**
  - `>` 是把输出写入**文件**
- `less` 适合看大文件，`cat` 适合看小文件（一屏能装下的）
- `grep` 默认区分大小写，加 `-i` 才忽略大小写
- `grep -r` 会搜遍文件夹里所有文件，找代码里的某个函数/变量时非常好用
- `head` 不加 `-n` 默认显示前 10 行，`tail` 同理

---

## Day 11｜命令行进阶：find 查找 + 重定向深入 + 文件权限初识（2026-08-05）

### 今天学了什么
1. `find` — 在文件树中查找文件（按名字、按类型、递归）
2. 输出重定向深入：`>` / `2>` — 把正常输出和错误输出分别保存
3. 文件权限初识：`ls -l` 看权限,`chmod` 改权限

### 关键命令/概念
| 命令/概念 | 作用 | 示例 |
|-----------|------|------|
| `find 路径 -name "模式"` | 按文件名查找 | `find . -name "*.html"` |
| `find 路径 -type f` | 只查文件（不含目录） | `find . -type f` |
| `find 路径 -type d` | 只查目录 | `find . -type d` |
| `find 路径 -name "x" -delete` | 查找并删除 | `find . -name "*.tmp" -delete` |
| `命令 > 文件` | 重定向标准输出（覆盖） | `find . > list.txt` |
| `命令 >> 文件` | 重定向标准输出（追加） | `find . >> list.txt` |
| `命令 2> 文件` | 重定向错误输出 | `find /no-path 2> err.log` |
| `命令 > out 2> err` | 分别保存正常/错误输出 | `find . > out.log 2> err.log` |
| `ls -l` | 详细列表（含权限、所有者、修改时间） | 见下方权限说明 |
| `chmod +x 文件` | 给文件加执行权限 | `chmod +x myscript.sh` |
| `chmod -x 文件` | 去掉执行权限 | `chmod -x myscript.sh` |

### 文件权限解读（`ls -l` 第一列）
```
-rw-r--r-- 1 用户 用户  1234 Aug  5 10:00 index.html
↑↑↑↑↑↑↑↑↑↑
第一个字符 — 文件类型:
  - = 普通文件
  d = 目录（directory）
  l = 符号链接（link）

接下来 9 个字符 — 分 3 组,每组 3 位:
  -rw-  →  所有者权限（user）
  -r--  →  组权限（group）
  -r--  →  其他人权限（others）

每组三位:r（读=4）w（写=2）x（执行=1）
  rwx = 4+2+1 = 7
  rw- = 4+2+0 = 6
  r-- = 4+0+0 = 4
  --- = 0
```

### 三种输出流（0 / 1 / 2）
| 编号 | 名称 | 含义 | 默认去向 |
|------|------|------|----------|
| 0 | stdin | 标准输入 | 键盘 |
| 1 | stdout | 标准输出（正常） | 屏幕 |
| 2 | stderr | 标准错误输出 | 屏幕 |

- `>` 默认重定向 1（stdout）
- `2>` 重定向 2（stderr）
- 两者独立,可以用一个命令同时管两种输出

### 今日实操流程（按顺序敲）
```bash
# 1. 进入项目文件夹
cd /f/SoloProgramming/my-first-project

# 2. 创建今天的练习文件夹
mkdir practice-day11
cd practice-day11

# 3. 创建测试文件和子目录
mkdir docs code
touch index.html about.html style.css script.js
touch docs/note1.txt docs/note2.md
touch code/main.py code/utils.py

# 4. 用 find 查找所有 .html 文件
find . -name "*.html"
# 预期输出:
# ./about.html
# ./index.html

# 5. 只查文件类型（不算目录）
find . -type f

# 6. 只查目录
find . -type d
# 预期: . ./docs ./code

# 7. 精确查找
find . -name "style.css"
# 预期: ./style.css

# 8. 创建带 TODO 的测试文件（配合 grep -r 复习 Day 10）
echo "TODO: finish html" > todo1.txt
echo "done" > done.txt
echo "TODO: test" > todo2.txt

# 9. 递归搜索所有含 TODO 的文件
grep -r "TODO" .
# 预期: ./todo1.txt 和 ./todo2.txt 各显示一行

# 10. 输出重定向 - 把 find 结果保存到文件
find . -name "*.html" > html-list.txt
cat html-list.txt

# 11. 错误输出重定向 - 试图访问不存在的路径
find /nonexistent-folder 2> errors.log
cat errors.log
# 预期: 错误信息被存到 errors.log,终端上不会刷错误

# 12. 同时重定向正常输出和错误输出
find . -name "*.html" > found.log 2> errors.log
cat found.log
cat errors.log

# 13. 文件权限初识 - 查看权限
ls -l index.html
# 预期看到类似: -rw-r--r-- 1 用户 用户 大小 Aug  5 10:00 index.html

# 14. 创建一个 shell 脚本
echo '#!/bin/bash
echo "Hello from my script"' > myscript.sh

# 15. 查看脚本的权限
ls -l myscript.sh
# 预期: -rw-r--r-- (没有 x 权限)

# 16. 试图执行它（现在会失败）
./myscript.sh
# 预期: bash: ./myscript.sh: Permission denied

# 17. 给脚本加执行权限
chmod +x myscript.sh
ls -l myscript.sh
# 预期: -rwxr-xr-x (有 x 权限了)

# 18. 执行脚本
./myscript.sh
# 预期: Hello from my script

# 19. 试着去掉执行权限
chmod -x myscript.sh
ls -l myscript.sh
# 预期: 又变成 -rw-r--r--

# 20. 清理练习文件
cd ..
rm -r practice-day11
```

### 验收标准
- [ ] 用 `find . -name "*.html"` 找到项目里的所有 HTML 文件
- [ ] 用 `find -type f` 和 `find -type d` 区分文件和目录
- [ ] 用 `find . -name "x" > file` 把查找结果保存到文件
- [ ] 用 `2>` 把错误输出重定向到文件
- [ ] 理解 `>` 和 `2>` 的区别（一个管正常输出，一个管错误输出）
- [ ] 用 `ls -l` 查看文件权限,能说出第一列的含义
- [ ] 用 `chmod +x` 给脚本加执行权限,执行成功
- [ ] 用 `chmod -x` 去掉执行权限
- [ ] Git add → commit → push 推上 GitHub

### 注意事项
- `find .` 的 `.` 表示"当前目录"，可以替换成任意路径
- `>` 默认就是 `1>`（管 stdout），`2>` 是单独管 stderr，两者独立不冲突
- 改了脚本不生效？先 `ls -l` 看最后一位有没有 `x`
- 文件权限今天理解概念即可，数字写法 `chmod 755` 放后面学
- W2 命令行快结束了，Day 12-14 会收尾 + 综合实战

---

## Day 12｜命令行实用工具 + 文本处理管线（2026-08-06）

### 今天学了什么
1. `which` / `whereis` — 查找命令的可执行文件在哪
2. `man` / `--help` — 查看命令的官方说明书
3. `sort` — 对文本行排序
4. `uniq` — 去除相邻的重复行（常跟 sort 搭配）
5. `diff` — 比较两个文件的差异
6. `tar` — 打包和解压文件
7. 综合实战：把 sort / uniq / grep / wc 用管道串起来处理文本

### 关键命令/概念
| 命令 | 作用 | 示例 |
|------|------|------|
| `which 命令名` | 查找命令的可执行文件路径 | `which git` → `/usr/bin/git` |
| `whereis 命令名` | 查找命令+手册页位置 | `whereis ls` |
| `命令 --help` | 快速查看命令用法（终端内） | `ls --help` |
| `man 命令名` | 查看完整手册页（分页） | `man grep`，按 q 退出 |
| `sort 文件名` | 按字母顺序排序每行 | `sort names.txt` |
| `sort -n 文件名` | 按数字大小排序 | `sort -n scores.txt` |
| `sort -r 文件名` | 逆序排序 | `sort -r names.txt` |
| `uniq 文件名` | 去除相邻重复行 | `uniq names.txt` |
| `sort 文件 \| uniq` | 先排序再去重（经典组合） | `sort names.txt \| uniq` |
| `sort 文件 \| uniq -c` | 去重并统计每行出现次数 | `sort log.txt \| uniq -c` |
| `diff 文件A 文件B` | 比较两个文件的差异 | `diff v1.txt v2.txt` |
| `tar -cvf 包.tar 文件夹` | 打包（不压缩） | `tar -cvf backup.tar practice-day12` |
| `tar -zcvf 包.tar.gz 文件夹` | 打包+压缩 | `tar -zcvf backup.tar.gz practice-day12` |
| `tar -xvf 包.tar` | 解包 | `tar -xvf backup.tar` |
| `tar -zxvf 包.tar.gz` | 解压 .tar.gz | `tar -zxvf backup.tar.gz` |

### `which` vs `whereis`
```
which git     → 只显示可执行文件路径（1个）
whereis git   → 显示可执行文件 + 源码 + 手册页路径（多个）
```
日常用 `which` 就够了，想知道命令装在哪用它。

### `--help` vs `man`
```
ls --help     → 快速参考，直接在终端显示，看完就结束
man ls        → 完整说明书，进入 less 分页模式，按 q 退出
```
- 忘了某个参数怎么用？先 `--help` 扫一眼，不够再 `man`
- `man` 里按 `/关键词` 可以搜索，跟 less 一样

### `sort` + `uniq` 经典管线
```
原始文件（有重复行）
  ↓ sort        先排序（uniq 只去相邻重复行，所以必须先 sort）
  ↓ uniq        去重
  ↓ uniq -c     去重 + 统计出现次数
  ↓ sort -rn    按出现次数从大到小排
```
这是命令行里最常见的文本处理管线，记住这个组合。

### `diff` 输出解读
```
diff v1.txt v2.txt
```
输出格式：
- `<` 开头的行 = 只在 v1.txt 里有
- `>` 开头的行 = 只在 v2.txt 里有
- 用来对比"改了什么"非常直观

### `tar` 参数记忆
| 参数 | 全称 | 作用 |
|------|------|------|
| `-c` | create | 创建新包 |
| `-x` | extract | 解包 |
| `-v` | verbose | 显示过程（处理的文件名） |
| `-f` | file | 指定包文件名（必须放最后，紧跟文件名） |
| `-z` | gzip | 压缩/解压（.tar.gz 格式） |

记忆口诀：**c 创建 / x 解开 / v 看过程 / f 指定文件 / z 压缩**

### 今日实操流程（按顺序敲）
```bash
# 1. 进入项目文件夹
cd /f/SoloProgramming/my-first-project

# 2. 创建今天的练习文件夹
mkdir practice-day12
cd practice-day12

# === which / whereis ===

# 3. 查找常用命令的位置
which git
which ls
which python
# 预期：显示每个命令的可执行文件路径

# 4. 用 whereis 看更多信息
whereis git
# 预期：显示可执行文件 + 手册页路径

# === --help / man ===

# 5. 快速查看 sort 的用法
sort --help
# 扫一眼有哪些参数

# 6. 查看 grep 的完整手册
man grep
# 进入后按 /pattern 搜索，按 q 退出

# === sort / uniq ===

# 7. 创建一个有重复行的文件
echo "apple" > fruits.txt
echo "banana" >> fruits.txt
echo "apple" >> fruits.txt
echo "cherry" >> fruits.txt
echo "banana" >> fruits.txt
echo "apple" >> fruits.txt

# 8. 直接 sort 看排序效果
sort fruits.txt
# 预期：apple apple apple banana banana cherry

# 9. sort + uniq 去重
sort fruits.txt | uniq
# 预期：apple banana cherry

# 10. sort + uniq -c 去重并计数
sort fruits.txt | uniq -c
# 预期：
#       3 apple
#       2 banana
#       1 cherry

# 11. 按出现次数从大到小排
sort fruits.txt | uniq -c | sort -rn
# 预期：3 apple → 2 banana → 1 cherry

# 12. 数字排序测试
echo "10" > nums.txt
echo "2" >> nums.txt
echo "1" >> nums.txt
echo "20" >> nums.txt
sort nums.txt       # 字母序：1 10 2 20（错！）
sort -n nums.txt    # 数字序：1 2 10 20（对！）

# === diff ===

# 13. 创建两个文件来对比
echo "line1" > v1.txt
echo "line2" >> v1.txt
echo "line3" >> v1.txt

echo "line1" > v2.txt
echo "line2-modified" >> v2.txt
echo "line3" >> v2.txt
echo "line4" >> v2.txt

# 14. 比较差异
diff v1.txt v2.txt
# 预期：
# 2c2
# < line2
# ---
# > line2-modified
# 3a4
# > line4

# === tar 打包/解压 ===

# 15. 把今天的练习文件打包（不压缩）
tar -cvf day12-backup.tar fruits.txt nums.txt v1.txt v2.txt
# 预期：终端显示打包的文件名，生成 day12-backup.tar

# 16. 查看包里有什么（不解开）
tar -tf day12-backup.tar

# 17. 解包到新文件夹
mkdir restore
cd restore
tar -xf ../day12-backup.tar
ls
# 预期：fruits.txt nums.txt v1.txt v2.txt
cd ..

# 18. 打包+压缩（.tar.gz）
tar -zcvf day12-backup.tar.gz fruits.txt nums.txt
# 生成 day12-backup.tar.gz（比 .tar 小）

# 19. 解压 .tar.gz
mkdir restore2
cd restore2
tar -zxvf ../day12-backup.tar.gz
ls
cd ..

# === 综合实战：用管道串起来 ===

# 20. 创建一个模拟日志文件
echo "ERROR: database connection failed" > app.log
echo "INFO: server started" >> app.log
echo "ERROR: timeout" >> app.log
echo "WARN: memory high" >> app.log
echo "ERROR: database connection failed" >> app.log
echo "INFO: request completed" >> app.log
echo "ERROR: timeout" >> app.log

# 21. 找出所有 ERROR 行，去重，统计次数，按次数从大到小排
grep "ERROR" app.log | sort | uniq -c | sort -rn
# 预期：
#       2 ERROR: database connection failed
#       2 ERROR: timeout

# 22. 统计日志总行数
wc -l app.log

# 23. 只看最后 3 行日志
tail -n 3 app.log

# 24. 清理练习文件
cd ..
rm -r practice-day12
```

### 验收标准
- [ ] 用 `which git` 找到 git 的安装路径
- [ ] 用 `命令 --help` 和 `man 命令` 查看帮助，能从 man 里用 q 退出
- [ ] 用 `sort 文件 | uniq` 对有重复行的文件去重
- [ ] 用 `sort 文件 | uniq -c` 统计每行出现次数
- [ ] 用 `sort -n` 做数字排序（理解不加 -n 的问题）
- [ ] 用 `diff` 比较两个文件的差异，能看懂 `<` 和 `>` 的含义
- [ ] 用 `tar -cvf` 打包文件，用 `tar -xvf` 解包
- [ ] 用 `tar -zcvf` 打包压缩，用 `tar -zxvf` 解压
- [ ] 完成综合实战：`grep "ERROR" app.log | sort | uniq -c | sort -rn`
- [ ] Git add → commit → push 推上 GitHub

### 注意事项
- `uniq` 只去**相邻**的重复行，不排序直接 uniq 没用 —— 永远 `sort | uniq` 一起用
- `sort` 默认按字母序，排数字必须加 `-n`，否则 10 会排在 2 前面
- `tar -f` 后面必须紧跟文件名，不能 `tar -cvf backup .tar`（中间有空格会出错）
- `diff` 输出里 `<` 是左边文件（第一个参数），`>` 是右边文件（第二个参数）
- `.tar` 只是打包不压缩，`.tar.gz` 是打包+压缩，体积更小
- Day 14 是第一次阶段性考试（60分钟闭卷），Day 13 是综合复习日

---

## Day 13｜W2 综合复习日：Day 8-12 命令大串联（2026-08-07）

### 今天学了什么
1. W2 命令行全部命令的总览速查表（Day 8-12 一张图）
2. 综合实操：在项目里建一个迷你"代码分析工作流"
3. 写第一个可复用的 shell 脚本
4. Day 14 阶段性考试预热（题型 / 范围 / 注意事项）

### Day 8-12 命令总览（背诵用）
| 分类 | 命令 | 作用 | 哪天学的 |
|------|------|------|----------|
| 文件操作 | `cp` | 复制 | Day 8 |
| 文件操作 | `cp -r` | 复制文件夹 | Day 8 |
| 文件操作 | `mv` | 移动/重命名 | Day 8 |
| 文件操作 | `rm` | 删除文件 | Day 8 |
| 文件操作 | `rm -r` | 删除文件夹 | Day 8 |
| 文件操作 | `mkdir` | 创建目录 | Day 3 |
| 文件操作 | `touch` | 创建空文件 | Day 3 |
| 文件操作 | `cat` | 查看文件全部内容 | Day 9 |
| 文件操作 | `less` | 分页查看大文件 | Day 10 |
| 文件操作 | `head -n N` | 看前 N 行 | Day 10 |
| 文件操作 | `tail -n N` | 看后 N 行 | Day 10 |
| 文本处理 | `echo "x" > 文件` | 写入（覆盖） | Day 9 |
| 文本处理 | `echo "x" >> 文件` | 追加 | Day 9 |
| 文本处理 | `sort` | 字母序排序 | Day 12 |
| 文本处理 | `sort -n` | 数字序排序 | Day 12 |
| 文本处理 | `sort -r` | 逆序排序 | Day 12 |
| 文本处理 | `uniq` | 去相邻重复行 | Day 12 |
| 文本处理 | `uniq -c` | 去重+计数 | Day 12 |
| 搜索查找 | `grep "词" 文件` | 文本搜索 | Day 10 |
| 搜索查找 | `grep -i` | 忽略大小写搜索 | Day 10 |
| 搜索查找 | `grep -n` | 显示行号 | Day 10 |
| 搜索查找 | `grep -r "词" 文件夹` | 递归搜索 | Day 10 |
| 搜索查找 | `find 路径 -name "x"` | 按名查找 | Day 11 |
| 搜索查找 | `find 路径 -type f` | 只查文件 | Day 11 |
| 搜索查找 | `find 路径 -type d` | 只查目录 | Day 11 |
| 搜索查找 | `which 命令` | 查命令位置 | Day 12 |
| 搜索查找 | `whereis 命令` | 查命令+手册 | Day 12 |
| 统计 | `wc -l` | 统计行数 | Day 10 |
| 比较 | `diff A B` | 比较文件差异 | Day 12 |
| 打包 | `tar -cvf 包.tar 文件` | 打包 | Day 12 |
| 打包 | `tar -xvf 包.tar` | 解包 | Day 12 |
| 打包 | `tar -zcvf 包.tar.gz 文件` | 打包压缩 | Day 12 |
| 打包 | `tar -zxvf 包.tar.gz` | 解压 | Day 12 |
| 权限 | `ls -l` | 查看权限 | Day 11 |
| 权限 | `chmod +x 文件` | 加执行权限 | Day 11 |
| 权限 | `chmod -x 文件` | 去执行权限 | Day 11 |
| 重定向 | `命令 > 文件` | stdout 重定向 | Day 9/11 |
| 重定向 | `命令 >> 文件` | stdout 追加 | Day 9/11 |
| 重定向 | `命令 2> 文件` | stderr 重定向 | Day 11 |
| 帮助 | `命令 --help` | 快速帮助 | Day 12 |
| 帮助 | `man 命令` | 完整手册 | Day 12 |
| 效率 | Tab 键 | 路径/文件名补全 | Day 9 |
| 效率 | `↑/↓` | 历史命令 | Day 9 |
| 效率 | `history` | 历史列表 | Day 9 |
| 效率 | `*` `?` | 通配符 | Day 9 |
| 基础 | `pwd` | 显示当前路径 | Day 2 |
| 基础 | `ls` | 列目录 | Day 2 |
| 基础 | `cd` | 切换目录 | Day 2 |

### 综合实操：代码分析工作流

目标：在 `my-first-project` 里建一份模拟"项目报告"，把 W2 学的命令全部用上。

```bash
# 1. 进入项目文件夹
cd /f/SoloProgramming/my-first-project

# 2. 创建今天的练习文件夹
mkdir practice-day13
cd practice-day13

# 3. 模拟一个项目目录结构（含子目录）
mkdir src docs logs
touch src/index.html src/style.css src/script.js src/app.py
touch docs/readme.md docs/todo.txt
echo "ERROR: connection failed" > logs/app.log
echo "INFO: server started" >> logs/app.log
echo "ERROR: timeout" >> logs/app.log
echo "WARN: memory high" >> logs/app.log
echo "ERROR: connection failed" >> logs/app.log

# 4. 用 find 看完整文件树
find . -type f

# 5. 用 find 把所有源码列出来
find . -name "*.html" -o -name "*.css" -o -name "*.js" -o -name "*.py"

# 6. 用 find 统计每种文件类型有几个
find . -name "*.html" | wc -l   # HTML 数
find . -name "*.css" | wc -l    # CSS 数
find . -name "*.js" | wc -l     # JS 数

# 7. 用 grep -r 找所有 TODO
grep -rn "TODO" .

# 8. 用 grep -r 找所有 ERROR，统计次数
grep "ERROR" logs/app.log | sort | uniq -c | sort -rn

# 9. 用重定向把项目报告输出到文件
echo "=== 项目代码分析报告 ===" > report.txt
echo "" >> report.txt
echo "HTML 文件数: $(find . -name '*.html' | wc -l)" >> report.txt
echo "CSS 文件数: $(find . -name '*.css' | wc -l)" >> report.txt
echo "JS 文件数: $(find . -name '*.js' | wc -l)" >> report.txt
echo "" >> report.txt
echo "=== 错误日志统计 ===" >> report.txt
grep "ERROR" logs/app.log | sort | uniq -c | sort -rn >> report.txt
echo "" >> report.txt
echo "=== TODO 列表 ===" >> report.txt
grep -rn "TODO" . >> report.txt

# 10. 查看报告
cat report.txt

# 11. 把整个项目打包备份
cd ..
tar -zcvf project-day13-backup.tar.gz practice-day13
ls -lh project-day13-backup.tar.gz

# 12. 清理（可选，保留 report.txt 看看效果也行）
cd practice-day13
cd ..
rm -r practice-day13
rm project-day13-backup.tar.gz
```

### 第一个 shell 脚本（进阶，可选）

如果你想真正"自动化"上面的流程，可以写一个脚本：

```bash
# 创建脚本文件
cd /f/SoloProgramming/my-first-project
mkdir scripts
cat > scripts/analyze.sh << 'EOF'
#!/bin/bash
# 项目代码分析脚本 —— 统计代码文件、错误日志、生成报告
# 用法：./analyze.sh <项目目录>

TARGET=${1:-.}
REPORT="analysis-report.txt"

echo "=== 项目分析：$TARGET ===" > $REPORT
echo "" >> $REPORT

# 统计各类代码文件
for ext in html css js py md; do
    count=$(find "$TARGET" -name "*.$ext" 2>/dev/null | wc -l)
    echo "$ext 文件数: $count" >> $REPORT
done

echo "" >> $REPORT
echo "=== 错误日志（如果有） ===" >> $REPORT
find "$TARGET" -name "*.log" -exec grep -h "ERROR" {} \; 2>/dev/null | sort | uniq -c | sort -rn >> $REPORT

echo "报告已生成：$REPORT"
cat $REPORT
EOF

# 加执行权限
chmod +x scripts/analyze.sh

# 测试运行
./scripts/analyze.sh /f/SoloProgramming/my-first-project
```

> 上面的 `<< 'EOF' ... EOF` 叫"heredoc"，是把多行内容写进文件的一种写法。今天先照着敲，理解意思即可，不需要深究语法。

### Day 14 考试预热
- **时间：** 60 分钟闭卷
- **范围：** Day 1-13 全部内容，重点最近两周（W2 命令行）
- **题型：** 选择 6×4=24 + 判断 4×3=12 + 填空 4×6=24 + 实例 2×20=40（满分 100）
- **形式：** HTML 考卷文件，本地答题
- **闭卷要求：** 考试中**不能查笔记、不能搜索**
- **重点复习方向：**
  - Day 8：`cp -r`、`mv`、`rm -r` 的区别
  - Day 9：`>` vs `>>` 区别、Tab 补全
  - Day 10：管道符 `|`、grep `-i/-n/-r`、wc `-l`
  - Day 11：find 三大参数、2> vs >、ls -l 权限解读、chmod +x
  - Day 12：sort+uniq 经典管线、-n/-r 参数、tar 参数记忆、which/whereis
- **不要死记硬背**，理解概念最重要 —— 命令是做事的工具，不是考试答案

### 验收标准
- [ ] 能口头（不查笔记）说出 W2 全部命令至少 20 个
- [ ] 完成综合实操：项目代码分析工作流，能生成 report.txt
- [ ] （可选）写一个 analyze.sh 脚本，能跑起来
- [ ] 复习 Day 14 考试范围，心里有数
- [ ] Git add → commit → push 推上 GitHub

### 注意事项
- 今天是复习日，**不要焦虑** —— 重点是串联，不是学新东西
- 如果某些命令忘了，回看学习笔记的 Day 8-12 章节复习，但**不要死背**
- 明天考试前不要再学新东西，看一眼上面那张总览表就够了
- shell 脚本部分是选学，不强求

---


## Day 14｜第一次阶段性考试（2026-08-08）- PASSED

### 考试结果
- 总分 **100 / 100**
- 16 题全部正确（选择 6 + 判断 4 + 填空 4 + 实例 2）
- 无薄弱点
- 考卷路径：`工作区/考试/Day14-exam.html`

### 考后发现
- **判分系统说明**：实例题按**关键词命中**评分，不验证命令是否能执行
  - 例子：第 15 题答案 `cd /f/SoloProgramming/my-first-project find ...` 缺 `&&`，bash 会报 `too many arguments`
  - 但因为包含所有关键词被判 +20
  - **启示**：判分系统给分不等于命令正确，必须自己在 bash 实测
- **第 15 题实测发现的两个 bug**：
  1. `find -name "*.js" > js-files.txt | wc -l` 返回 0
     - 原因：`>` 优先级高于 `|`，find 的 stdout 被全部劫持写进文件，管道后面拿到空输入
     - 修复 1：`find ... > file` 换行 `wc -l file`
     - 修复 2：`find ... | tee file | wc -l`（tee 三通管分流）
  2. `cd xxxx find` 缺 `&&` 或换行
     - bash 把第二个词当 cd 的参数，报 `too many arguments`
     - 修复：`cd xxxx && find ...` 或换行分两行写

### 下一步
- Day 15 进入 W3（PATH + roadmap + FCC 预热）

---


## Day 15｜W3 升级日：PATH 环境变量 + roadmap 扫图 + FCC 启动（2026-08-08）

### 今天学什么
1. **PATH 环境变量**：为什么 `ls` 直接能跑，`./analyze.sh` 必须带 `./`
2. **临时 vs 永久改 PATH**：export + .bashrc
3. **roadmap.sh 前端路线图**：看 90 天主线序列
4. **FCC 账号注册**：拿到 RWD 课程的入口

### 核心概念：PATH
- `echo $PATH` 显示一串用 `:` 分隔的目录，比如 `/usr/bin:/c/Program Files/Git/mingw64/bin:...`
- shell 执行命令时按顺序去这些目录里找可执行文件
- 自己写的脚本默认不在 PATH 里，所以必须 `./xxx.sh`
- `which 命令` 显示该命令在 PATH 哪个目录里找到的

### 改 PATH 的三种方式
| 方式 | 写法 | 生效范围 | 用法 |
|---|---|---|---|
| 临时（当前会话） | `export PATH="新目录:$PATH"` | 当前 bash 关掉就失效 | 测试用 |
| 永久（~/.bashrc） | 在文件里加 `export PATH=...` | 当前用户的所有 bash | 个人常用 |
| 永久（系统级） | 改 Windows 环境变量 | 所有用户 | 谨慎用 |

### FCC 启动清单
1. 访问 https://www.freecodecamp.org/ 注册
2. 进入 https://www.freecodecamp.org/learn/responsive-web-design/ 看 RWD 课程
3. RWD 课程结构（按顺序）：Basic HTML / Basic CSS / Applied Accessibility / Responsive Design Principles / CSS Flexbox / CSS Grid / 5 个项目 → 拿认证

### 验收标准
- [ ] `hello` 命令临时加 PATH 后能跑
- [ ] `~/.bashrc` 加一行后重开 bash 能 `hello` 直接跑
- [ ] 说出 roadmap.sh 前端路线的 3-5 个主干节点
- [ ] FCC 账号注册成功，能看到 RWD 课程页
- [ ] 加餐题：在 bash 里实测重定向 vs 管道的区别，结果与笔记一致

### 实战踩坑记录（2026-08-09 实操发现）

**坑 1：`which` 在 Git Bash 上对无扩展名脚本有盲区**
- 现象：`hello` 脚本有 `+x` 权限，PATH 里也有对应目录，但 `which hello` 报 `no hello in (...)`
- 原因：Git Bash (MSYS2) 的 `which` 对没有 Windows 可执行扩展名（.exe / .bat / .cmd）的纯文本脚本有"盲区"，即使文件有 `+x` 权限也看不见
- 解决：用 `type 命令名` 验证（bash 内置命令，直接读 bash 的查找逻辑，永远准）
  ```bash
  type hello
  # 输出: hello is /f/SoloProgramming/my-first-project/practice-day15/hello
  # 或: hello is hashed (/f/.../hello)  ← bash 缓存了路径，正常现象
  ```
- 记忆：**Git Bash 上验证命令路径用 `type`，不要迷信 `which`**

**坑 2：`$HOME` 在 Git Bash 上不等于项目所在盘**
- 现象：`.bashrc` 里写 `export PATH="$HOME/SoloProgramming/my-first-project/practice-day15:$PATH"`，`source` 后 `hello` 仍找不到
- 原因：Git Bash 的 `$HOME` = `/c/Users/jijij/`（C 盘用户目录），不是项目所在的 `/f/`
  - `$HOME/SoloProgramming/...` 展开成 `/c/Users/jijij/SoloProgramming/...` —— 这个路径不存在
- 解决：写绝对路径 `/f/SoloProgramming/...`，不要用 `$HOME` 拼项目路径
- 记忆：**macOS/Linux 上 `$HOME` 通常是项目根，但 Git Bash on Windows 上 `$HOME` 在 C 盘，跟 F 盘项目无关**

**PATH 任务验收结论**
- 临时 `export PATH="$PWD:$PATH"` + 在对应目录下 → `hello` 成功 ✓
- 关掉重开 bash → `hello` 失败（临时 PATH 失效）✓
- `.bashrc` 写入 + `source` → 因 `$HOME` 坑未跑通，但机制已验证（cat 看到内容、source 不报错）
- 核心概念全部理解：会话级 PATH / 配置文件级 PATH / $PWD / which vs type

### 注意事项
- FCC 5 个项目必须自己写，抄袭会被撤证封号
- W3 的 path 章节跟 W2 的命令是连续的一条线，今天的"为什么 ls 能直接跑"就是 W2 命令行实操的最后一块拼图
- **Git Bash 验证命令路径用 `type`，不用 `which`**（which 对无扩展名脚本有盲区）
- **Git Bash 的 `$HOME` 指向 C 盘用户目录，不等于项目所在盘**，写 .bashrc 时用绝对路径

---

## Day 16｜.bashrc 修复 + FCC HTML 初探 + roadmap 节点深入（2026-08-10）

### 今天学什么
1. **修复 Day 15 的 .bashrc 坑**：用绝对路径替换 `$HOME`，让永久 PATH 真正生效
2. **FCC RWD HTML 模块初探**：动手做第一个 Workshop，体验交互式学习平台
3. **roadmap.sh 节点深入**：点开 HTML / CSS 节点详情，看推荐资源和说明

### 核心概念：.bashrc 永久 PATH 修复

Day 15 的坑回顾：`.bashrc` 里写了 `export PATH="$HOME/SoloProgramming/...:$PATH"`，但 Git Bash 的 `$HOME` = `/c/Users/jijij/`（C 盘），不是项目所在的 `/f/`。展开后路径不存在，所以永久 PATH 没生效。

**修复方案**：不用 `$HOME`，直接写绝对路径。

### .bashrc 修复实操

```bash
# 1. 查看 .bashrc 当前内容（确认 Day 15 写的那行还在）
cat ~/.bashrc

# 2. 用 Cursor 打开 .bashrc 编辑
#    方法：Cursor → File → Open File → 输入 C:\Users\jijij\.bashrc
#    如果文件不存在，先创建：touch ~/.bashrc

# 3. 找到 Day 15 写的那行，改成绝对路径：
#    旧（错误）：
#    export PATH="$HOME/SoloProgramming/my-first-project/practice-day15:$PATH"
#    新（正确）：
export PATH="/f/SoloProgramming/my-first-project/practice-day15:$PATH"

# 4. 保存文件，回到 bash 执行：
source ~/.bashrc

# 5. 验证：直接输入 hello（不带 ./）
hello
# 预期：能执行，输出脚本内容

# 6. 终极验证：关掉 Git Bash 窗口，重新打开一个新的
hello
# 预期：仍然能执行 → 永久 PATH 生效！
```

### .bashrc 是什么

| 概念 | 说明 |
|------|------|
| `~/.bashrc` | bash 的个人配置文件，每次打开新 Git Bash 自动执行里面的命令 |
| `source ~/.bashrc` | 手动重新加载配置文件，不用关窗口重开 |
| `export PATH="目录:$PATH"` | 把新目录加到 PATH 最前面，`:$PATH` 保留原有路径 |
| 绝对路径 vs `$HOME` | Git Bash on Windows 的 `$HOME` 在 C 盘，项目在 F 盘，拼路径会出错 |

### FCC HTML 模块初探

| 信息 | 内容 |
|------|------|
| 课程地址 | https://www.freecodecamp.org/learn/2022/responsive-web-design/ |
| 第一个模块 | **Basic HTML** > **Build a Curriculum Outline** (Workshop，137 步) |
| 学习方式 | 左边看说明 + 右边写代码 → 点"Run"验证 → 通过自动进下一关 |
| 进度保存 | 登录后自动保存，随时可暂停 |
| 目标 | 今天做前 15-20 步（今天实际完成 11 步），体验平台交互式学习的感觉 |

> ⚠️ **修正**：之前写错了 FCC 第一项。新版 FCC 的第一个 Workshop 不是 "Learn HTML by Building a Cat Photo App"，而是 "Build a Curriculum Outline"。FCC 课程结构经常变动，以实际页面为准。

**FCC 交互课操作流程**：
1. 打开课程链接，登录账号
2. 点击 **Basic HTML** 进入 → 找到 **Build a Curriculum Outline** Workshop
3. 左边是说明文字，告诉你这步要做什么
4. 中间是代码编辑区，按说明修改代码
5. 改完点下方绿色 **"Run"** 按钮
6. 通过后自动跳下一步，顶部有进度条

### roadmap.sh 节点深入

| 操作 | 步骤 |
|------|------|
| 打开路线图 | https://roadmap.sh/frontend |
| 点 HTML 节点 | 路线图上找 HTML（黄色方块），点击弹出详情 |
| 看 CSS 节点 | 同上，点击 CSS 节点 |
| 看 JavaScript 节点 | 同上，点击 JavaScript 节点 |

每个节点弹出框里有：
- 节点说明（这个技术是什么）
- 社区推荐的教程链接（不用全看，知道有就行）
- 相关子节点（更细分的知识点）

**目标**：能说出 HTML / CSS / JavaScript 各自是干嘛的，心里有张前端地图。

### 验收标准
- [ ] `.bashrc` 修复后，`source` 加载，直接输入命令名能跑
- [ ] **关掉重开 Git Bash**，命令名仍能跑（永久 PATH 真正生效）
- [ ] FCC HTML 模块完成前 15-20 步（或完成第一个 Workshop）
- [ ] roadmap.sh 点开 HTML / CSS / JavaScript 节点，看过详情页
- [ ] Git add → commit → push 推上 GitHub（学习笔记更新）

### 注意事项
- **`.bash_profile` vs `.bashrc`（Day 16 踩坑结论）**：
  - Git Bash 每次打开是**登录 shell**，启动时读 `.bash_profile`，**不读 `.bashrc`**
  - `.bashrc` 只对 `source ~/.bashrc` 手动执行生效，关掉重开就没了
  - **永久环境变量 → 写 `.bash_profile`**；临时测试 → `export` 或 `source ~/.bashrc`
- 用绝对路径是最稳妥的写法，不依赖 `$HOME` 等变量（Git Bash 上变量值可能和你预期不同）
- FCC 新版结构：Basic HTML → Build a Curriculum Outline 是第一项 Workshop（137 步），不是 Cat Photo App
- FCC 课程不用赶进度，理解每一步在做什么比刷步数重要
- roadmap.sh 节点详情里的链接不用全看，当前阶段知道"有这些方向"就够了
- W3 还在预热，W4（D22-28）开始系统刷 FCC RWD 交互课

---

## Day 17｜FCC HTML 继续推进 + roadmap CSS 节点深入 + PATH 复盘（2026-08-11）

### 今天学什么
1. **FCC HTML**：继续 Build a Curriculum Outline Workshop（步 12 → 步 50+）
2. **roadmap CSS 节点**：前端路线图里 CSS 的三大阶段（基础 → 布局 → 响应式）
3. **PATH 复盘**：确认永久 PATH 正常工作，理解 `.bash_profile` vs `.bashrc`

### FCC HTML 进度
- Workshop：**Build a Curriculum Outline**（Basic HTML 模块，共 137 步）
- Day 16 完成：步 1-11
- Day 17 目标：步 12-50+
- 课程地址：https://www.freecodecamp.org/learn/2022/responsive-web-design/
- 操作：左边说明 + 右边代码 + Run 验证 → 自动跳下一步

### roadmap.sh CSS 节点（完整结构，2026-08-11 修正）

roadmap 上 CSS 的实际节点树（远比"三大阶段"复杂）：

**核心路径（标黄，W4-7 系统学）：**
- **Learn the Basics**：Box Model / Selectors / Specificity / Colors / Pseudo-classes & Pseudo-elements / Fonts & Typography / Positioning / Display / Cascade & Inheritance / CSS Variables / Media Queries
- **Making Layouts**：Floats → Flexbox → CSS Grid → Responsive Design
- FCC 对应：Basic CSS 模块覆盖大部分 Basics + Flexbox + Grid + Responsive Design Principles

**进阶路径（拿到 RWD 认证后再说）：**
- CSS Preprocessors：Sass / Less / PostCSS
- CSS Frameworks：Tailwind CSS / Bootstrap
- CSS Architecture：BEM / OOCSS / SMACSS
- Modern CSS：Container Queries / @layer / Nesting

> **当前策略**：扫图时看到这些节点知道"存在"就行，核心路径才是 W4-7 要啃的。进阶节点一根毛都别碰，别让地图把你吓到。

### PATH 复盘确认
```bash
# 验证脚本直接可用
hello
# type 查看来源（Git Bash 上 type 比 which 准）
type hello
# 确认 .bash_profile 用的是绝对路径
cat ~/.bash_profile | grep practice-day15
```

### 验收标准
- [ ] FCC HTML Workshop 做到步 50 以上
- [ ] roadmap CSS 节点打开看过，能说出三大阶段
- [ ] PATH 复查通过（hello 直接能跑）
- [ ] Git add → commit → push 推上 GitHub

### 注意事项
- `.bash_profile` = Git Bash 登录时自动读；`.bashrc` = 手动 source 才生效
- FCC 步骤不用赶，理解每一步比刷步数重要
- roadmap CSS 子节点现在只是"看地图"，内容细节由 FCC 教
- push 前记得开 Clash（127.0.0.1:7892）

---

## Day 18｜FCC HTML 推进 + roadmap JavaScript 预览 + 命令行速刷（2026-08-18）

### 今天学什么
1. **FCC HTML**：继续 Build a Curriculum Outline Workshop（步 50 → 90+）
2. **roadmap JavaScript 节点**：预览 JS 路线图子节点，知道未来要学什么
3. **命令行速刷**：一周没碰，10 分钟保持手感，Day 28 考试要来了

> ⚠️ 距离上次学习（Day 17，2026-08-11）过了一周，命令行手感可能生疏，今天有速刷任务。

### FCC HTML 进度
- Workshop：**Build a Curriculum Outline**（Basic HTML 模块，共 137 步）
- Day 17 完成：步 50 附近
- Day 18 目标：步 90+，能做完更好
- 课程地址：https://www.freecodecamp.org/learn/2022/responsive-web-design/
- 如果做完整个 Workshop，进入 Basic HTML 模块的下一个项目

### roadmap.sh JavaScript 节点预览

JavaScript 在 roadmap 上的子节点结构（预览，不深挖）：

**核心子方向（标黄）：**
- **Syntax & Basics**：变量、类型、运算符、条件、循环、函数
- **DOM Manipulation**：用 JS 操作网页元素（增删改查 HTML）
- **Events**：点击、输入、滚动等用户交互
- **AJAX / Fetch**：从服务器获取数据
- **ES6+**：现代 JS 语法（箭头函数、解构、模板字符串等）

> **当前策略**：跟 CSS 一样，扫图知道"有这些方向"就行。JS 系统学习在拿到 RWD 认证之后（W14 开始）。现在只做 FCC 的 HTML/CSS 部分。

### 命令行速刷（10 分钟）

一周没碰命令行，跑一遍这些保持手感：

```bash
# 1. 进入项目文件夹
cd /f/SoloProgramming/my-first-project

# 2. 创建练习文件夹
mkdir practice-day18
cd practice-day18

# 3. 写文件 + 追加
echo "line 1" > test.txt
echo "line 2" >> test.txt
echo "line 3" >> test.txt

# 4. 查看内容
cat test.txt

# 5. 搜索 + 行号
grep -n "line" test.txt

# 6. 统计行数
wc -l test.txt

# 7. 管道：排序 + 去重 + 计数
echo "apple" >> fruits.txt
echo "banana" >> fruits.txt
echo "apple" >> fruits.txt
sort fruits.txt | uniq -c

# 8. 查看 PATH
echo $PATH

# 9. 验证 hello 命令还在
type hello

# 10. 清理
cd ..
rm -r practice-day18
```

### 验收标准
- [ ] FCC HTML Workshop 做到步 90 以上，或完成整个 Workshop
- [ ] roadmap.sh JavaScript 节点打开看过，能说出 3 个子方向
- [ ] 命令行速刷全部跑通，没有卡住的
- [ ] Git add → commit → push 推上 GitHub

### 注意事项
- 一周没学，命令行可能生疏——速刷任务就是为了找回手感
- Day 28 是下一次阶段性考试（Day 14 后 14 天），范围 Day 1-27 全部
- roadmap JavaScript 节点现在只"看地图"，JS 系统学习在 W14 开始
- FCC 步骤不用赶，理解每一步比刷步数重要
- push 前记得开 Clash（127.0.0.1:7892）

---

